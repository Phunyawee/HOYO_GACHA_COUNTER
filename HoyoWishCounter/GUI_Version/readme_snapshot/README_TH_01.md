<div align="center">

# 🌠 Universal Hoyo Wish Counter (GUI Version)

[![Thai Badge](https://img.shields.io/badge/Lang-Thai-blue)](./README_TH_01.md)
[![English Badge](https://img.shields.io/badge/Lang-English-red)](./README_01.md)

**โปรแกรมนับ Pity กาชาสำหรับ Genshin Impact, Honkai: Star Rail และ Zenless Zone Zero**
เวอร์ชั่นใหม่! มาพร้อมหน้าต่างโปรแกรม (GUI) ใช้งานง่าย ค้นหาไฟล์ให้อัตโนมัติ และ Export ลง Excel ได้

![GUI Result](../../screenshots/GUI_Result.png)
*(หน้าตาโปรแกรมที่ดูง่าย พร้อมสรุป Pity ทันที)*

</div>

---

## 📂 รายชื่อไฟล์
| ชื่อไฟล์ | คำอธิบาย |
| :--- | :--- |
| **Start_GUI.bat** | ▶️ **ตัวเปิดโปรแกรม:** ดับเบิ้ลคลิกไฟล์นี้เพื่อเริ่มใช้งาน |
| **App.ps1** | 🖼️ **ไฟล์หน้าต่าง:** สคริปต์หลักสำหรับแสดงผล GUI |
| **HoyoEngine.ps1** | ⚙️ **ระบบหลัก:** ไฟล์คำนวณและดึงข้อมูล (ห้ามลบ หรือรันตรงๆ) |
| **config.json** | 📝 **ตั้งค่า:** สำหรับใส่ Discord Webhook (ไม่บังคับ) |

---

## 🚀 วิธีการใช้งาน

### 📌 ขั้นตอนที่ 1: เตรียม Link กาชา
โปรแกรมนี้ทำงานโดยการอ่าน Cache ในเครื่อง เพื่อความปลอดภัยและแม่นยำ

1.  **เข้าเกม** (Genshin, HSR หรือ ZZZ)
2.  เปิดหน้า **ประวัติการอธิษฐาน (History)** ในเกม
3.  รอให้รายการโหลดเสร็จ แล้ว **ปิดเมนู**
    *   *การทำแบบนี้จะทำให้เกมสร้างไฟล์ `data_2` ที่มี Link ล่าสุดขึ้นมา*

---

### ⚡ ขั้นตอนที่ 2: รันโปรแกรม

#### 1️⃣ เปิดโปรแกรม
ดับเบิ้ลคลิกที่ไฟล์ **`Start_GUI.bat`** รอสักครู่ หน้าต่างโปรแกรมจะเด้งขึ้นมา

#### 2️⃣ เลือกเกม และ หาไฟล์
1.  กดปุ่มเกมที่ต้องการเช็คด้านบน (เช่น **Genshin**)
2.  กดปุ่ม **"Auto-Detect"** (สีฟ้า)
    *   โปรแกรมจะพยายามหาไฟล์ `data_2` ในเครื่องให้อัตโนมัติ
    *   *ถ้าหาไม่เจอ:* ให้กดปุ่ม "Browse..." แล้วเลือกไฟล์ในโฟลเดอร์ `webCaches` ของเกมด้วยตัวเอง

![Auto Detect](../../screenshots/GUI_Main.png)

#### 3️⃣ เริ่มดึงข้อมูล
1.  เลือกตู้ที่ต้องการ (Banner) หรือแนะนำให้เลือก **"* FETCH ALL"** (ดึงทุกตู้)
2.  กดปุ่ม **"START FETCHING"** (สีเขียวใหญ่)
3.  โปรแกรมจะเริ่มทำงาน และแสดงรายการที่ดึงได้ในช่องสีดำด้านล่าง

#### 4️⃣ ดูผลลัพธ์ และ บันทึกไฟล์
*   **ช่อง Log:** จะสรุปรายการ 5 ดาว (สีทอง) พร้อมบอกเลข Pity
*   **Discord:** หากตั้งค่าไว้ ผลสรุปจะเด้งเข้า Discord ทันที
*   **Export:** กดปุ่ม **">> Export History to CSV"** เพื่อเซฟประวัติทั้งหมดออกมาเป็นไฟล์ Excel (.csv)

![Result Screen](../../screenshots/GUI_Running.png)

---

## 💬 การตั้งค่า Discord (ไม่บังคับ)
หากต้องการให้ส่งผลเข้า Discord Server ของคุณ:

1.  สร้างไฟล์ชื่อ `config.json` ในโฟลเดอร์เดียวกัน
2.  ใส่ข้อมูลตามรูปแบบนี้:

```
json
{
    "webhook_url": "https://discord.com/api/webhooks/YOUR_WEBHOOK_URL_HERE"
}
```
![Discord Report](../../screenshots/Discord_Result2.png)

*(ตัวอย่างรายงานที่จะส่งเข้า Discord เมื่อดึงข้อมูลเสร็จ)*

## 🛠️ การแก้ปัญหาเบื้องต้น
**Q: ขึ้น Error "HoyoEngine.ps1 not found!"**
A: ตรวจสอบว่าไฟล์ **App.ps1** และ **HoyoEngine.ps1** อยู่ในโฟลเดอร์เดียวกัน และให้รันผ่าน Start_GUI.bat เสมอ

**Q: ขึ้น Error "AuthKey not found" หรือ "Link Expired"**
A: ลิงก์ในไฟล์ Cache หมดอายุแล้ว (ปกติอยู่ได้ประมาณ 1 ชม.)
วิธีแก้: เข้าเกมไปเปิดหน้าประวัติกาชาใหม่ แล้วกลับมากด Auto-Detect และ Start Fetching อีกครั้ง

**Q: กด Auto-Detect แล้วหาไฟล์ไม่เจอ**
A: หากคุณลงเกมไว้ที่ Drive อื่น หรือ Folder แปลกๆ ระบบอาจหาไม่เจอ ให้กดปุ่ม **"Browse..."** เพื่อเลือกไฟล์ data_2 เอง


## 📜 เครดิต
**พัฒนาโดย:** ใช้ PowerShell ร่วมกับ Windows Forms
**เทคนิคการดึงข้อมูล:** อ้างอิง Logic การแกะ Cache ไฟล์ data_2 จากคอมมูนิตี้ (paimon.moe)